# EconFlow Configuration Reference

> **Auto-generated** from `src/econflow/config/models.py`.
> Do not edit this file manually — run `econflow docs config` to regenerate.

EconFlow projects are configured through three YAML files:

| File | Purpose |
|------|---------|
| `config.yaml` | Project metadata, data source, sample period, variable names |
| `models.yaml` | Estimator specifications (one entry per regression) |
| `outputs.yaml` | Table and figure output settings |

All files support strict validation.  Unknown keys cause an error.
Run `econflow validate config/` to check all three files at once.


## `config.yaml`

### `ProjectConfig`

Root model for ``config.yaml``.


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `project` | `ProjectModel` | *(required)* | Project identification metadata. |
| `data` | `DataModel` | *(required)* | Data source path and column names. |
| `sample` | `SampleModel` | `SampleModel(start_year=1990, end_year=2020, min_obs_per_entity=0)` | Temporal and cross-sectional sample bounds. |
| `variables` | `VariablesModel` | *(required)* | Dependent variable, regressor list, and instrument list. |

#### `project`

Top-level project identification block (``project:`` in config.yaml).


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `name` | `str` | *(required)* | Human-readable project name.  Used in provenance stamps, report headers, and replication package metadata. 
  *Examples:*
    - `'getting_started'`
    - `'my_tfp_study'` |
| `version` | `str` | `"0.1.0"` | Semantic-version string for the project.  Bump when you materially change a specification so archived results remain traceable to the config that produced them. 
  *Examples:*
    - `'0.1.0'`
    - `'1.2.3'` |
| `description` | `str` | `""` | Free-text description of the project and its research question. 
  *Examples:*
    - `'Firm investment and fixed effects — Grunfeld panel (1935-1954)'` |
| `authors` | `list[AuthorModel]` | `[]` | List of project authors.  Each entry must have a ``name``. 
  *Examples:*
    - `[{'name': 'Jane Smith', 'email': 'jane@example.com'}]` |
| `output_dir` | `str` | `"outputs"` | Root directory for pipeline output artefacts (tables, figures, certificates).  Relative paths are resolved from the project root. 
  *Examples:*
    - `'outputs'`
    - `'results'` |

##### `project.authors`

A project author entry inside ``project.authors``.


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `name` | `str` | *(required)* | Author's full name. 
  *Examples:*
    - `'Jane Smith'` |
| `email` | `str` | `""` | Author's contact email address.  Optional. 
  *Examples:*
    - `'jane@example.com'` |

#### `data`

Data source configuration (``data:`` in config.yaml).


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `path` | `str` | *(required)* | Path to the processed panel CSV.  Relative paths are resolved from config.yaml's parent directory. 
  *Examples:*
    - `'data/processed/panel.csv'`
    - `'../data/grunfeld.csv'` |
| `entity_col` | `str` | *(required)* | Name of the cross-sectional identifier column in the CSV (e.g. country ISO-3 code, firm id).  Used by all panel estimators. 
  *Examples:*
    - `'country'`
    - `'firm'`
    - `'entity'` |
| `time_col` | `str` | *(required)* | Name of the time-period column in the CSV (e.g. calendar year, quarter).  Must be numeric or parseable. 
  *Examples:*
    - `'year'`
    - `'quarter'`
    - `'time'` |
| `required_columns` | `list[str]` | `[]` | Whitelist of columns that must be present in the CSV.  ``econflow validate --data`` checks each column is present. Typically includes entity_col, time_col, dependent, and all regressors. 
  *Examples:*
    - `['country', 'year', 'ln_gdp', 'ln_capital']` |

#### `sample`

Temporal sample bounds (``sample:`` in config.yaml).


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `start_year` | `int` | `1990` | First calendar year included in the estimation sample (inclusive). Rows with ``time_col < start_year`` are excluded before estimation. 
  *Examples:*
    - `1990`
    - `2000`
    - `2010` |
| `end_year` | `int` | `2020` | Last calendar year included in the estimation sample (inclusive). Rows with ``time_col > end_year`` are excluded before estimation. 
  *Examples:*
    - `2020`
    - `2022`
    - `2019` |
| `min_obs_per_entity` | `int` | `0` | Drop entities (countries / firms) with fewer than this many non-missing observations.  Set to 0 to keep all entities. 
  *Examples:*
    - `0`
    - `5`
    - `10` |

#### `variables`

Variable definitions (``variables:`` in config.yaml).


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `dependent` | `str` | *(required)* | Name of the outcome (dependent) variable.  Must be a column in the CSV.  This variable appears on the left-hand side of every regression model. 
  *Examples:*
    - `'invest'`
    - `'ln_gdp_pc'`
    - `'tfp_growth'` |
| `regressors` | `list[str]` | *(required)* | List of right-hand-side variable names (excluding fixed effects and time/entity dummies, which are handled automatically).  All entries must be columns in the CSV. 
  *Examples:*
    - `['value', 'capital']`
    - `['ln_ai_index', 'ln_capital', 'ln_labor']` |
| `instruments` | `list[str]` | `[]` | Excluded instruments for IV / 2SLS estimation.  Must be columns in the CSV.  Leave empty for non-IV models. 
  *Examples:*
    - `[]`
    - `['distance_to_equator', 'colonial_origin']` |
| `controls` | `list[str]` | `[]` | Additional control variables.  Included in all models but not highlighted in the main coefficient table. 
  *Examples:*
    - `[]`
    - `['population', 'trade_openness']` |


---

## `models.yaml`

### `ModelsConfig`

Root model for ``models.yaml``.


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `models` | `list[ModelSpec]` | *(required)* | Ordered list of regression model specifications.  Must contain at least one entry.  Each entry's ``id`` must be unique within this list. 
  *Examples:*
    - `[{'id': 'baseline', 'estimator': 'FE', 'dependent': 'y', 'regressors': ['x1', 'x2']}]` |

#### `models`

A single regression model specification (one entry in ``models:`` list).


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `id` | `str` | *(required)* | Unique model identifier.  Used as a key in outputs.yaml, in table column headers, and in provenance stamps.  Must start with a letter and contain only letters, digits, underscores, and hyphens. 
  *Examples:*
    - `'pooled_ols'`
    - `'entity_fe'`
    - `'twfe_robust'` |
| `label` | `str` | `""` | Human-readable display name for the model.  Appears in table headers and figure legends.  Defaults to the ``id`` if empty. 
  *Examples:*
    - `'Pooled OLS'`
    - `'Entity FE'`
    - `'Two-Way FE'` |
| `estimator` | `str` | *(required)* | Registry key of the estimator to use.  Must match a registered estimator ID (see ``econflow info`` for the full list).  Case-insensitive aliases: OLS→ols, FE→fe, TWFE→twfe, RE→re, FD→fd, IV→iv. 
  *Examples:*
    - `'OLS'`
    - `'FE'`
    - `'twfe'`
    - `'iv'` |
| `dependent` | `str` | *(required)* | Dependent variable for this model.  Typically matches ``variables.dependent`` in config.yaml, but may differ for robustness checks with alternative outcome variables. 
  *Examples:*
    - `'invest'`
    - `'ln_gdp_pc'` |
| `regressors` | `list[str]` | *(required)* | List of right-hand-side variables for this model.  Should be a subset of (or equal to) ``variables.regressors`` in config.yaml. 
  *Examples:*
    - `['value', 'capital']`
    - `['ln_ai_index', 'ln_capital']` |
| `entity_effects` | `bool` | `false` | Include entity (within) fixed effects.  Required for FE and TWFE estimators.  Ignored by OLS. 
  *Examples:*
    - `True`
    - `False` |
| `time_effects` | `bool` | `false` | Include time-period fixed effects.  Set to ``true`` for TWFE. Ignored by OLS. 
  *Examples:*
    - `True`
    - `False` |
| `cluster` | `str` | `""` | Column to cluster standard errors by.  Typical values: 'entity' (cluster by cross-sectional unit) or a column name.  Leave empty for heteroskedasticity-robust (HC) standard errors. 
  *Examples:*
    - `'entity'`
    - `'country'`
    - `''` |
| `description` | `str` | `""` | Free-text annotation for this specification.  Appears in table footnotes. 
  *Examples:*
    - `'Baseline pooled OLS — ignores firm heterogeneity.'` |


---

## `outputs.yaml`

### `OutputsConfig`

Root model for ``outputs.yaml``.


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `outputs` | `OutputsBlock` | *(required)* | All output configuration lives under this top-level key. |

#### `outputs`

The ``outputs:`` block in outputs.yaml.


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `base_dir` | `str` | *(required)* | Root directory for all output artefacts.  Subdirectories (tables/, figures/) are created relative to this path. 
  *Examples:*
    - `'outputs'`
    - `'../results'` |
| `tables` | `TablesModel` | *(required)* | Table generation settings. |
| `figures` | `FiguresModel` | `FiguresModel(dir='outputs/figures', enabled=True)` | Figure generation settings. |

##### `outputs.tables`

Table output settings (``outputs.tables:`` in outputs.yaml).


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `dir` | `str` | `"outputs/tables"` | Directory (relative to project root) where table files are written. 
  *Examples:*
    - `'outputs/tables'`
    - `'results/tables'` |
| `formats` | `list[str]` | `['csv', 'latex']` | List of output formats to produce for every table.  Supported: ``csv``, ``latex``, ``markdown``, ``html``, ``json``. 
  *Examples:*
    - `['csv', 'latex']`
    - `['csv', 'markdown']` |
| `comparison_table` | `ComparisonTableModel` | *(required)* | Main coefficient comparison table settings. |

###### `outputs.tables.comparison_table`

Settings for the main regression comparison table.


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `filename` | `str` | *(required)* | Output filename stem (without extension).  The renderer appends the appropriate extension (.csv, .tex, .md, etc.). 
  *Examples:*
    - `'table_main_results'`
    - `'table_fe_investment'` |
| `models` | `list[str]` | `[]` | Ordered list of model IDs to include as columns.  Each ID must exist in models.yaml.  Leave empty to include all models. 
  *Examples:*
    - `['pooled_ols', 'entity_fe', 'twfe']` |
| `stars` | `bool` | `true` | Append statistical-significance stars to coefficient estimates (*** p<0.01, ** p<0.05, * p<0.1). 
  *Examples:*
    - `True`
    - `False` |
| `se_type` | `'robust' | 'clustered' | 'classical'` | `"robust"` | Standard error type shown in parentheses below each coefficient.  ``robust``: heteroskedasticity-robust HC SEs.  ``clustered``: cluster-robust SEs (cluster column set per model).  ``classical``: OLS standard errors (not recommended for panels). 
  *Examples:*
    - `'robust'`
    - `'clustered'` |

##### `outputs.figures`

Figure output settings (``outputs.figures:`` in outputs.yaml).


| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `dir` | `str` | `"outputs/figures"` | Directory where figure files are written. 
  *Examples:*
    - `'outputs/figures'` |
| `enabled` | `bool` | `true` | Master switch for figure generation.  Set to ``false`` to skip all figures (useful for fast runs). 
  *Examples:*
    - `True`
    - `False` |
