# Reference Outputs (moved)

The paper-specific reference outputs have been relocated to:

```
examples/ai_productivity_paper/reference_outputs/
```

This directory is kept as a placeholder so that existing tooling that
scans `tests/fixtures/` does not receive unexpected errors.  All
content comparisons and regression assertions must point to the new
location above.
